name: Docs Sync

on:
  push:
    branches: [main]
    paths:
      - "lib/mcp/**"
      - "lib/graph/tool-handlers.ts"
      - "plugins/claude-code/**"
      - "scripts/generate-docs.ts"
  workflow_dispatch:

jobs:
  sync:
    name: Regenerate docs and open PR
    runs-on: ubuntu-latest
    if: github.repository == 'FrkAk/mymir'
    concurrency:
      group: docs-sync
      cancel-in-progress: false
    permissions:
      contents: read
    steps:
      - name: Checkout mymir
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Checkout mymir-docs
        uses: actions/checkout@v6
        with:
          repository: FrkAk/mymir-docs
          token: ${{ secrets.DOCS_REPO_TOKEN }}
          path: docs-repo
          persist-credentials: true

      - name: Regenerate docs
        run: bun scripts/generate-docs.ts --out docs-repo/content/docs

      - name: Open PR if changed
        uses: peter-evans/create-pull-request@v7
        with:
          path: docs-repo
          token: ${{ secrets.DOCS_REPO_TOKEN }}
          branch: docs-sync/auto
          delete-branch: true
          commit-message: "docs: sync generated reference from mymir"
          title: "docs: sync generated reference from mymir"
          body: |
            Automated regeneration triggered by FrkAk/mymir@${{ github.sha }}.
            Sources: MCP schemas, tool descriptions, or plugin skill files.
          signoff: false
